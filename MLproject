name: text-mining

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      max_percent: {type: float, default: 0.8}
      min_count: {type: int, default: 2}
      num_feats: {type: int, default: None}
    command: "python train.py {max_percent} {min_count} {num_feats}"